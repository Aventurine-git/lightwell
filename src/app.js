import{makePanel,panelToSVG}from"./generator.js";const $=s=>document.querySelector(s),stage=$("#stage"),seed=$("#seed"),count=$("#count"),lead=$("#lead"),hue=$("#hue"),sat=$("#sat"),val=$("#val"),color=$("#color"),sil=$("#silhouette");let current,silhouette="";function values(){return{seed:seed.value||"aventurine",cells:+count.value,lead:+lead.value,hue:+hue.value,saturation:+sat.value,value:+val.value}}function sync(){const v=values();current=makePanel(v);stage.innerHTML=`<div class="silhouette">${silhouette}</div>${panelToSVG(current)}`;if(!stage.querySelector("polygon")){stage.innerHTML+="<p class=renderError>Glass could not render.</p>"};for(const [id,n] of [["countLabel",v.cells],["leadLabel",v.lead],["hueLabel",v.hue],["satLabel",v.saturation],["valLabel",v.value]])$("#"+id).textContent=n;$("#seedLabel").textContent=v.seed;try{history.replaceState(null,"",`#${encodeURIComponent(v.seed)}`)}catch{}}function hexHue(hex){const r=parseInt(hex.slice(1,3),16)/255,g=parseInt(hex.slice(3,5),16)/255,b=parseInt(hex.slice(5),16)/255,max=Math.max(r,g,b),min=Math.min(r,g,b),d=max-min;let h=0;if(d){if(max===r)h=60*((g-b)/d%6);else if(max===g)h=60*((b-r)/d+2);else h=60*((r-g)/d+4)}return Math.round((h+360)%360)}function save(blob,name){const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),500)}const safe=s=>s.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"").slice(0,50)||"panel";$("#generate").onclick=sync;$("#surprise").onclick=()=>{seed.value=crypto.randomUUID().slice(0,8);hue.value=Math.floor(Math.random()*360);sync()};$("#svg").onclick=()=>save(new Blob([panelToSVG(current)],{type:"image/svg+xml"}),`lightwell-${safe(current.seed)}.svg`);color.oninput=()=>{hue.value=hexHue(color.value);sync()};[count,lead,hue,sat,val].forEach(e=>e.oninput=sync);seed.onkeydown=e=>{if(e.key==="Enter")sync()};sil.onchange=()=>{const f=sil.files[0];if(!f){silhouette="";return sync()}if(f.type!=="image/svg+xml"){alert("Choose an SVG silhouette.");sil.value="";return}const reader=new FileReader;reader.onload=()=>{const doc=new DOMParser().parseFromString(reader.result,"image/svg+xml"),root=doc.documentElement;root.querySelectorAll("script,foreignObject").forEach(n=>n.remove());root.querySelectorAll("*").forEach(n=>[...n.attributes].forEach(a=>{if(a.name.startsWith("on")||/^(href|xlink:href)$/i.test(a.name)&&!a.value.startsWith("#"))n.removeAttribute(a.name)}));silhouette=new XMLSerializer().serializeToString(root);sync()};reader.readAsText(f)};$("#clearSilhouette").onclick=()=>{silhouette="";sil.value="";sync()};seed.value=decodeURIComponent(location.hash.slice(1))||"different forms, one curious practice";sync();